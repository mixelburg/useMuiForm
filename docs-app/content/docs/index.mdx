---
title: Getting Started
description: Get started with useMuiForm - a lightweight React hook for form management with Material-UI
---

# useMuiForm

A lightweight custom React hook for **form management** with first-class support for **Material-UI (MUI)** and **MUI-X** components.

## Why?

This library was inspired by [react-hook-form](https://www.npmjs.com/package/react-hook-form).
It follows similar API patterns and design decisions, but is **purpose-built for MUI**.

## Installation

Choose your package manager:

```bash
npm i usemuiform
```

```bash
pnpm add usemuiform
```

```bash
yarn add usemuiform
```

```bash
bun add usemuiform
```

## Quick Start

Here's a simple example to get you started:

```tsx
import { useMuiForm } from 'usemuiform'
import { TextField, Button } from '@mui/material'

type FormState = {
  email: string
  name: string
}

function MyForm() {
  const { register, forceValidate } = useMuiForm<FormState>({
    defaultValues: { email: '', name: '' }
  })

  const handleSubmit = () => {
    if (forceValidate()) {
      console.log('Form is valid!')
    }
  }

  return (
    <form>
      <TextField
        label="Email"
        {...register('email', { required: true })}
      />
      <TextField
        label="Name"
        {...register('name', { required: true })}
      />
      <Button onClick={handleSubmit}>Submit</Button>
    </form>
  )
}
```

## Next Steps

- Check out the [Basic Usage](/docs/usage/basic) guide
- Learn about [Validation](/docs/usage/validation)
- Explore [Configuration](/docs/configuration)
- View the [API Reference](/docs/api/use-mui-form)

